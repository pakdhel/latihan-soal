// contoh 8.18
// konversi desimal to biner

#include <stdio.h>
#include <string.h>

int main()
{
    char biner[100]; 
/*
    int d, len1, len2;
    printf("Enter the decimal : ");scanf("%d", &d);

    while (d > 0)
    { 
        d = d % 2;
        switch (d)
        { 
            case 0: 
                for (int i=0;i<=  
        }
    }
*/
}